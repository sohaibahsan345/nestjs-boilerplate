export enum Roles {
    SUPER_ADMIN = 'super_admin',
    ADMIN = 'admin',
    USER = 'user',
}

export enum UserStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    UNDER_REVIEW = 'under-review',
    REJECTED = 'rejected'
}

export enum UserProfileStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive'
}